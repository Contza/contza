{
    "name": "react-playground",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "@contza/react": "*",
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "react-scripts": "5.0.1",
        "web-vitals": "^2.1.4"
    },
    "scripts": {
        "start": "react-scripts start",
        "build": "react-scripts build",
        "prestart": "yarn workspace @contza/client build && yarn workspace @contza/react build",
        "prebuild": "yarn workspace @contza/client build && yarn workspace @contza/react build"
    },
    "eslintConfig": {
        "extends": [
            "react-app"
        ]
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    }
}
